{
  "name": "ecoconnect-root",
  "version": "1.0.0",
  "description": "EcoConnect - Food Donation & Resource Sharing Platform",
  "main": "server.js",
  "scripts": {
    "install-all": "npm install && npm install --prefix backend && npm install --prefix frontend --legacy-peer-deps",
    "backend": "npm start --prefix backend",
    "frontend": "npm run dev --prefix frontend",
    "dev": "npx concurrently \"npm run backend\" \"npm run frontend\"",
    "build": "npm run install-all && npm run build --prefix frontend",
    "start": "node backend/server.js"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
